class Solution {
    public int countDigits(int num) {
        int ans = 0;
        while(num>0){
            int digit = num%10;
            if(digit!=0 && num%digit==0){
                ans++;
            }
            num = num/10;
        }
    }
}
